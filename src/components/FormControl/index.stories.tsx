import { FormControl } from '@components';

export default {
  title: 'Components/FormControl',
  component: FormControl,
};

export const Overview = () => {
  return <FormControl>Primary</FormControl>;
};
