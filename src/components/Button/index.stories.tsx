import { Button } from '@components';

export default {
  title: 'Components/Button',
  component: Button,
};

export const Overview = () => {
  return <Button variant="contained">Primary</Button>;
};
